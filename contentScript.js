
const init = function(){
  console.log('oui')


  
}
document.addEventListener('DOMContentLoaded', init());

/*
  const injectElement = document.createElement('div');
  injectElement.className = 'rustyZone-element';
  injectElement.innerHTML = 'Hello From the Rusty Zone Element';
  document.body.appendChild(injectElement);
  var titre_page = document.getElementsByTagName('title')[0];
  titre_page.innerHTML += " v2";
  var notes = document.getElementById("encart-notes");
  console.log(notes)
  window.onload = function() {
    var classe = document.getElementsByClassName('ng-star-inserted');
    console.log(classe[39]);
  }
  var user = sessionStorage.getItem('accounts');
  console.log(user['accounts'][0]['profile']['classe']['code'])
  console.log(JSON.parse(user)['accounts'][0]['profile']['classe']['code']);


  
  console.log(document.readyState)
  var id = document.getElementById('user-account-link');
  console.log(id)
  const injectElement = document.getElementsByClassName('main-container container-bg double-padding')[0].createElement('div');
  injectElement.className = 'manuel';
  injectElement.innerHTML = 'Mathématiques';
  document.body.appendChild(injectElement);*/
